# Media Sorter 📰

## About
A Python CLI tool to organize images and videos using metadata, with fallback handling for files without metadata.

## Features
- CLI interface with flexible argument passing.
- Organizes images and videos with and without EXIF metadata.
- Supports additional options such as backup, dry-run, and cleanup of empty input directories.
